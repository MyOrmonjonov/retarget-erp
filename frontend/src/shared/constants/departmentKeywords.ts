/** Substrings an employee's free-text `department` field must contain (case-insensitive) for
 * their tasks to appear on each department board (see useDeptTasks). Shared so the board pages
 * and the employee form's "does this match a board?" hint never drift apart. */
export const DESIGN_DEPT_KEYWORDS = ['dizayn', 'design'];
export const EDITING_DEPT_KEYWORDS = ['montaj', 'video', 'edit'];
