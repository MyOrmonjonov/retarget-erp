spring:
  datasource:
    url: jdbc:h2:mem:taskapp;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop

telegram:
  bot-token: ""

app:
  auth-secret: test-secret-at-least-long-enough
