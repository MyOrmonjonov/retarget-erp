# Copy to Caddyfile and set your real domain (or an IP-based one like 1-2-3-4.sslip.io).
# Caddy auto-provisions and renews a Let's Encrypt certificate for it on first request.
your-domain-or-ip.sslip.io {
    reverse_proxy frontend:80
}
